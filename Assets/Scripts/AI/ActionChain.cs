using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActionChain
{
    //public List<Action> actions = new List<Action>();
    //public IWorldState endWorldState;
    //public float cost;

    //public void RecalculateParams(IWorldState initialWorldState)
    //{
    //    cost = 0;
    //    endWorldState = initialWorldState.Clone();
    //    foreach (Action a in actions)
    //    {
    //        cost += a.EstimateCost(endWorldState);
    //        a.Execute(endWorldState);
    //    }

    //}
}